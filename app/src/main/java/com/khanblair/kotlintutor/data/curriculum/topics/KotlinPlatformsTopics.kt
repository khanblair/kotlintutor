package com.khanblair.kotlintutor.data.curriculum.topics

import com.khanblair.kotlintutor.model.CurriculumTopic
import com.khanblair.kotlintutor.model.QuizQuestion
import com.khanblair.kotlintutor.model.Recap

val kotlinPlatformsTopics: List<CurriculumTopic> = listOf(
    CurriculumTopic(
        id = "server-side-apps",
        title = "Server-side Apps",
        category = "Kotlin Platforms",
        recap = Recap(
            previousTopicTitle = "Kotlin from Java",
            recapText = "Kotlin and Java interop cleanly in both directions on the JVM — that same JVM foundation is what makes Kotlin a first-class backend language.",
            quickCheckQuestion = "What lets Kotlin call Java code, and Java call Kotlin code, in the same project?",
            quickCheckAnswer = "They both compile to JVM bytecode and share the same runtime.",
        ),
        explain = "Kotlin runs on the JVM, so it can do everything a backend Java app can do — and modern frameworks now treat Kotlin as a first-class citizen, not an afterthought. Ktor is JetBrains' own lightweight, asynchronous framework for building servers and HTTP clients in idiomatic Kotlin, built on coroutines from the ground up. Spring and Spring Boot, the dominant JVM server framework, added official Kotlin support years ago, including Kotlin-specific DSLs and null-safety-aware APIs. Kotlin's null safety catches whole classes of NullPointerException bugs at compile time, and coroutines make writing non-blocking, highly concurrent server code look like simple sequential code — both are a strong fit for the kind of I/O-heavy workloads (database calls, HTTP requests, message queues) that server apps live in. This app is itself a small example of that world: it uses Ktor Client (not a server, but the same library's client side) to make its network calls to the DeepSeek API.",
        example = "",
        keyPoints = listOf(
            "Kotlin/JVM is a first-class language for backend and server-side development.",
            "Ktor (JetBrains) and Spring/Spring Boot are the two most widely used server frameworks with strong Kotlin support.",
            "Null safety and coroutines make Kotlin well suited to concurrent, I/O-heavy server workloads.",
            "This app's own network layer uses Ktor Client to call the DeepSeek API — the same library family, used client-side.",
        ),
        quiz = listOf(
            QuizQuestion(
                id = "server-side-apps-q1",
                topicId = "server-side-apps",
                question = "Which framework is built by JetBrains specifically for Kotlin, using coroutines throughout?",
                options = listOf("Spring Boot", "Ktor", "Django", "Express"),
                correctIndex = 1,
                explanation = "Ktor is JetBrains' own lightweight, coroutine-based framework for servers and HTTP clients.",
            ),
            QuizQuestion(
                id = "server-side-apps-q2",
                topicId = "server-side-apps",
                question = "Why are Kotlin coroutines a good fit for server-side workloads?",
                options = listOf(
                    "They make code run on multiple servers automatically",
                    "They let you write non-blocking, concurrent I/O code that reads like sequential code",
                    "They replace the need for a database",
                    "They are only usable in Android apps",
                ),
                correctIndex = 1,
                explanation = "Coroutines make asynchronous, non-blocking I/O — the bread and butter of server workloads — easy to write and read.",
            ),
            QuizQuestion(
                id = "server-side-apps-q3",
                topicId = "server-side-apps",
                question = "How does this app itself use Ktor?",
                options = listOf(
                    "It runs a Ktor server locally on the device",
                    "It uses Ktor Client to make HTTP calls to the DeepSeek API",
                    "It doesn't use Ktor at all",
                    "It uses Ktor only for its build scripts",
                ),
                correctIndex = 1,
                explanation = "The app's network layer uses Ktor Client (the same library family used by Ktor servers) to call the DeepSeek API.",
            ),
        ),
        tutorFocus = "Ground this in the app's own codebase — it already uses Ktor Client for its DeepSeek calls, so 'Kotlin on the server' isn't abstract. Keep the framework comparison brief; the point is that Kotlin is a serious backend language, not a survey of every framework.",
    ),
    CurriculumTopic(
        id = "kotlin-js",
        title = "Kotlin/JavaScript",
        category = "Kotlin Platforms",
        recap = Recap(
            previousTopicTitle = "Server-side Apps",
            recapText = "Kotlin/JVM powers backend servers with frameworks like Ktor and Spring — but Kotlin's reach extends past the JVM entirely.",
            quickCheckQuestion = "What runtime do Kotlin/JVM server apps run on?",
            quickCheckAnswer = "The JVM (Java Virtual Machine).",
        ),
        explain = "Kotlin/JS compiles Kotlin source code to JavaScript, letting you write frontend or Node.js code in Kotlin instead. Historically there were two compiler backends: a legacy backend and the modern IR-based (Intermediate Representation) backend, which is now the standard and produces smaller, more optimized output. Kotlin/JS can target the browser, where you manipulate the DOM directly or use React wrappers from the kotlin-wrappers project to build React apps in Kotlin, or it can target Node.js for server-side JavaScript. Because real-world JS projects depend on npm packages, Kotlin/JS provides interop mechanisms — `external` declarations describe the shape of existing JavaScript/TypeScript APIs to the Kotlin compiler, and `@JsModule` annotations bind those declarations to specific npm packages, so Kotlin code can call into ordinary JavaScript libraries.",
        example = "",
        keyPoints = listOf(
            "Kotlin/JS compiles Kotlin to JavaScript, targeting either the browser or Node.js.",
            "The modern IR-based compiler backend replaced the legacy backend as the standard.",
            "kotlin-wrappers provides Kotlin bindings for React and other popular JS libraries.",
            "`external` declarations plus `@JsModule` let Kotlin code call existing npm packages.",
        ),
        quiz = listOf(
            QuizQuestion(
                id = "kotlin-js-q1",
                topicId = "kotlin-js",
                question = "What does the Kotlin/JS compiler produce?",
                options = listOf("JVM bytecode", "Native machine code", "JavaScript", "WebAssembly"),
                correctIndex = 2,
                explanation = "Kotlin/JS compiles Kotlin source to JavaScript, runnable in browsers or Node.js.",
            ),
            QuizQuestion(
                id = "kotlin-js-q2",
                topicId = "kotlin-js",
                question = "What does `external` combined with `@JsModule` let Kotlin/JS code do?",
                options = listOf(
                    "Run Kotlin code on iOS",
                    "Describe and call into existing JavaScript/npm libraries from Kotlin",
                    "Compile Kotlin to native binaries",
                    "Skip null safety checks entirely",
                ),
                correctIndex = 1,
                explanation = "`external` declarations describe a JS API's shape to the compiler, and `@JsModule` binds it to a specific npm package.",
            ),
            QuizQuestion(
                id = "kotlin-js-q3",
                topicId = "kotlin-js",
                question = "Which two environments can Kotlin/JS target?",
                options = listOf(
                    "Only the browser",
                    "Only Node.js",
                    "The browser and Node.js",
                    "Only Android WebView",
                ),
                correctIndex = 2,
                explanation = "Kotlin/JS output can run in the browser (DOM, React via kotlin-wrappers) or in a Node.js server environment.",
            ),
        ),
        tutorFocus = "Emphasize that this is the same Kotlin language and standard library concepts the learner already knows, just compiled to a different target. Don't dwell on the legacy vs IR backend history beyond one sentence — the IR backend is what matters today.",
    ),
    CurriculumTopic(
        id = "kotlin-wasm",
        title = "Kotlin/WASM",
        category = "Kotlin Platforms",
        recap = Recap(
            previousTopicTitle = "Kotlin/JavaScript",
            recapText = "Kotlin/JS compiles to JavaScript for the browser or Node.js, using `external`/@JsModule for npm interop — Kotlin/Wasm is a newer, different way to target the browser.",
            quickCheckQuestion = "What does Kotlin/JS compile Kotlin source code into?",
            quickCheckAnswer = "JavaScript.",
        ),
        explain = "Kotlin/Wasm compiles Kotlin to WebAssembly (Wasm), a low-level binary instruction format that browsers can execute at near-native speed. Compared to Kotlin/JS, Kotlin/Wasm often achieves faster startup and execution for compute-heavy code, because it skips much of the overhead of generating and running JavaScript. As of the Kotlin 2.x line, Kotlin/Wasm is newer and less mature than Kotlin/JS — Kotlin/JS has years of production use and broader library/tooling support, while Kotlin/Wasm is still catching up in ecosystem maturity even as its performance story improves. Kotlin/Wasm pairs naturally with Compose Multiplatform, letting the same declarative Compose UI code that runs on Android also render inside a browser tab via WebAssembly.",
        example = "",
        keyPoints = listOf(
            "Kotlin/Wasm compiles Kotlin to WebAssembly for near-native performance in the browser.",
            "It often offers faster startup/execution than Kotlin/JS, especially for compute-heavy code.",
            "It is newer and less mature than Kotlin/JS as of the Kotlin 2.x line.",
            "Compose Multiplatform can target Kotlin/Wasm to render Compose UIs directly in the browser.",
        ),
        quiz = listOf(
            QuizQuestion(
                id = "kotlin-wasm-q1",
                topicId = "kotlin-wasm",
                question = "What compilation target does Kotlin/Wasm produce?",
                options = listOf("JavaScript", "WebAssembly", "JVM bytecode", "Native ARM binaries"),
                correctIndex = 1,
                explanation = "Kotlin/Wasm compiles Kotlin source to WebAssembly, a low-level format browsers run at near-native speed.",
            ),
            QuizQuestion(
                id = "kotlin-wasm-q2",
                topicId = "kotlin-wasm",
                question = "How does Kotlin/Wasm's maturity compare to Kotlin/JS as of the Kotlin 2.x line?",
                options = listOf(
                    "Kotlin/Wasm is older and more established",
                    "They were released at exactly the same time",
                    "Kotlin/Wasm is newer and less mature than Kotlin/JS",
                    "Kotlin/JS has been deprecated in favor of Kotlin/Wasm",
                ),
                correctIndex = 2,
                explanation = "Kotlin/Wasm is the newer of the two browser targets and, as of the 2.x line, still less mature than Kotlin/JS.",
            ),
            QuizQuestion(
                id = "kotlin-wasm-q3",
                topicId = "kotlin-wasm",
                question = "What UI framework can target Kotlin/Wasm to run Compose UIs in the browser?",
                options = listOf("Jetpack Compose for Android only", "Compose Multiplatform", "SwiftUI", "React"),
                correctIndex = 1,
                explanation = "Compose Multiplatform can compile to Kotlin/Wasm, letting the same declarative UI code render in a browser tab.",
            ),
        ),
        tutorFocus = "Position this as a sibling to Kotlin/JS rather than a replacement — same goal (run Kotlin in the browser), different underlying technology and maturity level. The Compose Multiplatform connection is worth a sentence since the learner will see it again later in this category.",
    ),
    CurriculumTopic(
        id = "kotlin-multiplatform",
        title = "Kotlin Multiplatform",
        category = "Kotlin Platforms",
        recap = Recap(
            previousTopicTitle = "Kotlin/WASM",
            recapText = "Kotlin/Wasm compiles Kotlin to WebAssembly for near-native browser performance, newer than Kotlin/JS — both are single-platform targets. Kotlin Multiplatform is the umbrella that lets one Kotlin codebase target several platforms at once.",
            quickCheckQuestion = "What binary format does Kotlin/Wasm compile Kotlin to?",
            quickCheckAnswer = "WebAssembly.",
        ),
        explain = "Kotlin Multiplatform (KMP) lets you share Kotlin code across platforms — JVM/Android, iOS (via Kotlin/Native), JS/Wasm, and desktop — while still writing platform-specific code exactly where you need it. The core mechanism for that split is `expect`/`actual`: you declare an `expect` function, class, or property in shared (\"common\") code describing what's needed, and each platform provides an `actual` implementation using whatever APIs are native to that platform. A typical KMP architecture shares the business logic, networking, and data layers in common code, then either builds a native UI per platform (SwiftUI on iOS, Jetpack Compose on Android) or shares the UI too using Compose Multiplatform.",
        example = """
            |// commonMain: declare what each platform must provide
            |expect fun platformName(): String
            |
            |// androidMain: Android's actual implementation
            |actual fun platformName(): String = "Android"
            |
            |// iosMain: iOS's actual implementation
            |actual fun platformName(): String = "iOS"
        """.trimMargin(),
        keyPoints = listOf(
            "KMP shares one Kotlin codebase across JVM/Android, iOS, JS/Wasm, and desktop.",
            "`expect` declares a signature in common code; each platform supplies a matching `actual` implementation.",
            "A common pattern: share business logic/networking/data, keep UI native (or shared via Compose Multiplatform).",
            "Platform-specific code stays isolated behind the `expect`/`actual` boundary, not scattered through shared code.",
        ),
        quiz = listOf(
            QuizQuestion(
                id = "kotlin-multiplatform-q1",
                topicId = "kotlin-multiplatform",
                question = "What is the purpose of an `expect` declaration in Kotlin Multiplatform?",
                options = listOf(
                    "It marks a function as deprecated",
                    "It declares a signature in common code that each platform must implement with `actual`",
                    "It runs a function only on Android",
                    "It imports a Java library into common code",
                ),
                correctIndex = 1,
                explanation = "`expect` declares what shared code needs; each platform's `actual` implementation fulfills it with platform-specific code.",
            ),
            QuizQuestion(
                id = "kotlin-multiplatform-q2",
                topicId = "kotlin-multiplatform",
                question = "In a typical KMP architecture, what is commonly shared in common code?",
                options = listOf(
                    "Only the UI layer",
                    "Business logic, networking, and the data layer",
                    "Nothing — KMP only shares build configuration",
                    "Only unit tests",
                ),
                correctIndex = 1,
                explanation = "KMP projects typically share business logic, networking, and data layers, with UI either native per-platform or shared via Compose Multiplatform.",
            ),
            QuizQuestion(
                id = "kotlin-multiplatform-q3",
                topicId = "kotlin-multiplatform",
                question = "Which platforms can Kotlin Multiplatform target?",
                options = listOf(
                    "Only Android",
                    "Only Android and iOS",
                    "JVM/Android, iOS, JS/Wasm, and desktop",
                    "Only server-side JVM",
                ),
                correctIndex = 2,
                explanation = "KMP can target JVM (including Android), iOS via Kotlin/Native, JS/Wasm, and desktop (JVM), sharing common code across all of them.",
            ),
        ),
        tutorFocus = "The `expect`/`actual` mechanism is the one concrete mental model to nail down here — walk through the example line by line. Keep the architecture discussion at a high level; deeper iOS/native details come in the next couple of topics.",
    ),
    CurriculumTopic(
        id = "kotlin-native",
        title = "Kotlin/Native",
        category = "Kotlin Platforms",
        recap = Recap(
            previousTopicTitle = "Kotlin Multiplatform",
            recapText = "KMP shares Kotlin code across platforms via `expect`/`actual`, with iOS support coming from Kotlin/Native — worth a closer look at what Kotlin/Native actually is.",
            quickCheckQuestion = "What two keywords does KMP use to declare a shared signature and its platform-specific implementation?",
            quickCheckAnswer = "`expect` and `actual`.",
        ),
        explain = "Kotlin/Native compiles Kotlin directly to native machine code using the LLVM compiler backend — no JVM is involved at runtime at all. Because there's no JVM, Kotlin/Native ships its own memory manager for automatic memory management, rather than relying on a JVM garbage collector. Its primary real-world use case is enabling iOS as a Kotlin Multiplatform target: Kotlin/Native is what lets shared KMP code run on iPhones and iPads, since iOS can't run JVM bytecode. Beyond iOS, Kotlin/Native can also produce standalone native binaries for other platforms like Linux, Windows, and macOS.",
        example = "",
        keyPoints = listOf(
            "Kotlin/Native compiles Kotlin straight to native machine code via LLVM, with no JVM at runtime.",
            "It has its own memory manager, separate from the JVM's garbage collector.",
            "Its main use case is powering the iOS target in Kotlin Multiplatform projects.",
            "It can also produce native binaries for Linux, Windows, and macOS.",
        ),
        quiz = listOf(
            QuizQuestion(
                id = "kotlin-native-q1",
                topicId = "kotlin-native",
                question = "What compiler backend does Kotlin/Native use to produce native machine code?",
                options = listOf("The JVM interpreter", "LLVM", "V8", "GCC only"),
                correctIndex = 1,
                explanation = "Kotlin/Native uses LLVM to compile Kotlin directly to native machine code, without a JVM.",
            ),
            QuizQuestion(
                id = "kotlin-native-q2",
                topicId = "kotlin-native",
                question = "How does Kotlin/Native manage memory?",
                options = listOf(
                    "It uses the JVM's garbage collector",
                    "It has its own memory manager, independent of the JVM",
                    "It requires manual memory management like C",
                    "It has no memory management at all",
                ),
                correctIndex = 1,
                explanation = "Since there's no JVM at runtime, Kotlin/Native provides its own memory manager rather than reusing the JVM garbage collector.",
            ),
            QuizQuestion(
                id = "kotlin-native-q3",
                topicId = "kotlin-native",
                question = "What is Kotlin/Native's primary real-world use case?",
                options = listOf(
                    "Compiling Kotlin to JavaScript",
                    "Enabling iOS as a target for Kotlin Multiplatform",
                    "Running Kotlin inside a JVM server",
                    "Replacing Gradle as a build tool",
                ),
                correctIndex = 1,
                explanation = "Kotlin/Native's most common use case is making iOS a viable KMP target, since iOS can't run JVM bytecode.",
            ),
        ),
        tutorFocus = "Connect this directly back to the previous topic: Kotlin/Native is the 'how' behind KMP's iOS support. The LLVM and memory-manager details are supporting facts, not the main point — the main point is 'this is what makes iOS possible.'",
    ),
    CurriculumTopic(
        id = "kotlin-scripting",
        title = "Kotlin Scripting",
        category = "Kotlin Platforms",
        recap = Recap(
            previousTopicTitle = "Kotlin/Native",
            recapText = "Kotlin/Native compiles to native machine code with its own memory manager, mainly to power iOS targets — a very different corner of Kotlin is scripting, where there's no separate app to compile at all.",
            quickCheckQuestion = "What compiler backend does Kotlin/Native use?",
            quickCheckAnswer = "LLVM.",
        ),
        explain = "Kotlin scripting lets you write `.kts` files — executable Kotlin scripts that run directly, without the explicit build-and-run cycle of a compiled app. You've already used Kotlin scripting throughout this curriculum without necessarily naming it: every `build.gradle.kts` file in a Kotlin project is a Kotlin script, using Kotlin's syntax and type safety to configure Gradle instead of Groovy. Unlike a compiled Kotlin/JVM application, where you build an artifact and then run it, a script is evaluated top-to-bottom as a single unit each time you run it — there's no separate \"build once, run many times\" artifact from the user's perspective, which makes scripting well suited to automation, one-off tools, and glue code rather than long-running applications.",
        example = "",
        keyPoints = listOf(
            "`.kts` files are Kotlin scripts, executed directly rather than built into a separate artifact.",
            "`build.gradle.kts` is a Kotlin script you've already been using throughout this curriculum.",
            "Scripts run top-to-bottom each time, unlike a compiled app's build-then-run lifecycle.",
            "Scripting suits automation and glue code, not long-running production applications.",
        ),
        quiz = listOf(
            QuizQuestion(
                id = "kotlin-scripting-q1",
                topicId = "kotlin-scripting",
                question = "What file extension identifies a Kotlin script?",
                options = listOf(".kt", ".kts", ".ks", ".ktscript"),
                correctIndex = 1,
                explanation = "Kotlin scripts use the `.kts` extension, distinguishing them from regular `.kt` source files.",
            ),
            QuizQuestion(
                id = "kotlin-scripting-q2",
                topicId = "kotlin-scripting",
                question = "Which file you've already been using in this curriculum is actually a Kotlin script?",
                options = listOf("MainActivity.kt", "build.gradle.kts", "AndroidManifest.xml", "CurriculumTopic.kt"),
                correctIndex = 1,
                explanation = "`build.gradle.kts` uses the `.kts` extension because it's a Kotlin script that configures Gradle.",
            ),
            QuizQuestion(
                id = "kotlin-scripting-q3",
                topicId = "kotlin-scripting",
                question = "What best describes scripting's execution model compared to a compiled Kotlin/JVM app?",
                options = listOf(
                    "Scripts must be compiled into a separate artifact before every run, just like an app",
                    "Scripts run top-to-bottom each time, without a separate build-then-run artifact step",
                    "Scripts can only run inside a browser",
                    "Scripts require the JVM to be absent",
                ),
                correctIndex = 1,
                explanation = "A script is evaluated top-to-bottom each time it runs, rather than being built once into an artifact and run repeatedly.",
            ),
        ),
        tutorFocus = "Anchor this in something the learner has already touched: point out that `build.gradle.kts` is itself a Kotlin script. That makes an otherwise abstract topic immediately concrete.",
    ),
    CurriculumTopic(
        id = "c-interop",
        title = "C Interoperability",
        category = "Kotlin Platforms",
        recap = Recap(
            previousTopicTitle = "Kotlin Scripting",
            recapText = "`.kts` scripts (like build.gradle.kts) run top-to-bottom without a separate build artifact — back on the Kotlin/Native side, there's a different interop story: calling C libraries directly.",
            quickCheckQuestion = "What extension do Kotlin scripts use, and what's a real example from this curriculum?",
            quickCheckAnswer = "`.kts`; e.g. build.gradle.kts.",
        ),
        explain = "Kotlin/Native includes a tool called cinterop that reads a C library's header files and generates matching Kotlin bindings automatically. Once generated, Kotlin/Native code can call C functions and read/write C structs as if they were native Kotlin declarations, with no JVM-style bridging layer needed. This matters because a huge amount of platform and native SDK functionality — from operating system APIs to hardware libraries — is exposed only through a C API, so cinterop is the practical way for Kotlin/Native projects to reach that functionality directly.",
        example = "",
        keyPoints = listOf(
            "cinterop is Kotlin/Native's tool for generating Kotlin bindings from C library headers.",
            "Generated bindings let Kotlin/Native code call C functions and use C structs directly.",
            "This is the standard way to wrap native/platform SDKs that only expose a C API.",
            "No JVM-style bridging layer is needed — Kotlin/Native talks to C at the native code level.",
        ),
        quiz = listOf(
            QuizQuestion(
                id = "c-interop-q1",
                topicId = "c-interop",
                question = "What does Kotlin/Native's cinterop tool do?",
                options = listOf(
                    "Converts Kotlin code into C code",
                    "Generates Kotlin bindings from a C library's header files",
                    "Compiles C code into JVM bytecode",
                    "Removes the need for a C compiler",
                ),
                correctIndex = 1,
                explanation = "cinterop reads a C library's headers and generates matching Kotlin declarations that Kotlin/Native code can call.",
            ),
            QuizQuestion(
                id = "c-interop-q2",
                topicId = "c-interop",
                question = "After cinterop generates bindings, how does Kotlin/Native code use the C library?",
                options = listOf(
                    "It can call the C functions and use C structs directly",
                    "It still has to go through a JVM bridge",
                    "It can only read C constants, not call functions",
                    "It must rewrite the C library in Kotlin",
                ),
                correctIndex = 0,
                explanation = "Once bindings exist, Kotlin/Native code calls C functions and uses C structs directly, without extra bridging.",
            ),
            QuizQuestion(
                id = "c-interop-q3",
                topicId = "c-interop",
                question = "Why is C interop important for Kotlin/Native?",
                options = listOf(
                    "Most native/platform SDKs only expose a C API, so it's needed to reach that functionality",
                    "C interop is required to run Kotlin on the JVM",
                    "It replaces the need for the expect/actual mechanism",
                    "It's only used for writing Gradle scripts",
                ),
                correctIndex = 0,
                explanation = "A lot of native platform functionality is only exposed via C APIs, so C interop is the practical bridge to that functionality.",
            ),
        ),
        tutorFocus = "Keep this practical: cinterop is a bridge, not a language feature the learner will write syntax for directly in this course. One sentence on 'why would I ever need this' (wrapping a native SDK) does more than mechanism detail.",
    ),
    CurriculumTopic(
        id = "swift-objc-interop",
        title = "Swift/Objective-C Interop.",
        category = "Kotlin Platforms",
        recap = Recap(
            previousTopicTitle = "C Interoperability",
            recapText = "cinterop generates Kotlin bindings from C headers so Kotlin/Native can call C directly — Kotlin/Native's interop story runs the other direction too: Swift and Objective-C can call into Kotlin.",
            quickCheckQuestion = "What Kotlin/Native tool generates Kotlin bindings from a C library's headers?",
            quickCheckAnswer = "cinterop.",
        ),
        explain = "Kotlin/Native compiles a Kotlin Multiplatform shared module into an Objective-C framework, which Swift or Objective-C code can import and call directly, just like any other native framework. This is precisely how a KMP project's shared business logic, networking, and data layer gets consumed from an iOS app written in Swift — the iOS app imports the generated framework and calls its exposed Kotlin classes and functions as native Swift APIs. Some translation happens automatically at this boundary: for example, Kotlin nullable types (`String?`) map to Swift Optionals (`String?`), and Kotlin naming conventions are adapted to read naturally from Swift/Objective-C.",
        example = "",
        keyPoints = listOf(
            "Kotlin/Native exports a KMP shared module as an Objective-C framework.",
            "Swift and Objective-C code import that framework and call Kotlin code as native APIs.",
            "This is the mechanism by which a KMP shared module reaches an iOS app's Swift code.",
            "Nullability and naming are translated automatically, e.g. Kotlin nullable types become Swift Optionals.",
        ),
        quiz = listOf(
            QuizQuestion(
                id = "swift-objc-interop-q1",
                topicId = "swift-objc-interop",
                question = "What does Kotlin/Native produce so Swift/Objective-C code can consume it?",
                options = listOf(
                    "A JAR file",
                    "An Objective-C framework",
                    "A .kts script",
                    "A JavaScript module",
                ),
                correctIndex = 1,
                explanation = "Kotlin/Native compiles the shared module into an Objective-C framework that Swift/Obj-C can import directly.",
            ),
            QuizQuestion(
                id = "swift-objc-interop-q2",
                topicId = "swift-objc-interop",
                question = "How does a Kotlin Multiplatform shared module typically reach a Swift-based iOS app?",
                options = listOf(
                    "Through cinterop C bindings",
                    "Via the Objective-C framework Kotlin/Native generates from the shared module",
                    "It cannot reach iOS at all",
                    "By rewriting the shared code in Swift",
                ),
                correctIndex = 1,
                explanation = "The Swift app imports the Objective-C framework produced from the KMP shared module and calls its Kotlin APIs directly.",
            ),
            QuizQuestion(
                id = "swift-objc-interop-q3",
                topicId = "swift-objc-interop",
                question = "What does a Kotlin nullable type like `String?` map to in Swift?",
                options = listOf("A crash at runtime", "A Swift Optional", "A force-unwrapped String", "An NSNull object"),
                correctIndex = 1,
                explanation = "Kotlin's nullable types are translated automatically to Swift Optionals at the interop boundary.",
            ),
        ),
        tutorFocus = "Tie this directly to the KMP topic from earlier: this is literally how the shared module the learner read about gets used from Swift. The nullable-to-Optional mapping is a nice concrete detail since the learner already understands Kotlin nullability deeply.",
    ),
    CurriculumTopic(
        id = "compose-multiplatform",
        title = "Compose Multiplatform",
        category = "Kotlin Platforms",
        recap = Recap(
            previousTopicTitle = "Swift/Objective-C Interop.",
            recapText = "Kotlin/Native exports KMP shared modules as Objective-C frameworks so Swift can call Kotlin logic directly — that's sharing logic with a native UI per platform. Compose Multiplatform goes a step further and shares the UI too.",
            quickCheckQuestion = "What does Kotlin/Native produce so Swift can consume a KMP shared module?",
            quickCheckAnswer = "An Objective-C framework.",
        ),
        explain = "Compose Multiplatform is JetBrains' extension of Jetpack Compose — the same declarative UI toolkit this app's own screens are built with — to targets beyond Android: iOS, desktop (JVM), and web via Kotlin/Wasm. It uses the exact same `@Composable` function model the learner already knows: you describe what the UI should look like for a given state, and the framework handles rendering it, on whichever platform it's running on. This is the key contrast with plain Kotlin Multiplatform: KMP alone typically shares business logic, networking, and data while each platform keeps its own native UI (SwiftUI on iOS, Jetpack Compose on Android); Compose Multiplatform additionally shares the UI layer itself, so the same `@Composable` screens can run across Android, iOS, desktop, and web with little or no platform-specific UI code.",
        example = "",
        keyPoints = listOf(
            "Compose Multiplatform extends Jetpack Compose beyond Android to iOS, desktop (JVM), and web (Kotlin/Wasm).",
            "It uses the same declarative `@Composable` function model this app's own UI is already built with.",
            "KMP alone shares logic with native UI per platform; Compose Multiplatform shares the UI too.",
            "The web target relies on Kotlin/Wasm to run Compose UIs in the browser.",
        ),
        quiz = listOf(
            QuizQuestion(
                id = "compose-multiplatform-q1",
                topicId = "compose-multiplatform",
                question = "What is Compose Multiplatform an extension of?",
                options = listOf("SwiftUI", "Jetpack Compose", "React", "Kotlin/Native"),
                correctIndex = 1,
                explanation = "Compose Multiplatform extends Jetpack Compose — the toolkit this app's Android UI uses — to other platforms.",
            ),
            QuizQuestion(
                id = "compose-multiplatform-q2",
                topicId = "compose-multiplatform",
                question = "What's the key difference between plain Kotlin Multiplatform and Compose Multiplatform?",
                options = listOf(
                    "KMP shares logic with native UI per platform; Compose Multiplatform shares the UI too",
                    "There is no difference, they are the same thing",
                    "Compose Multiplatform only works on Android",
                    "KMP shares UI but not logic",
                ),
                correctIndex = 0,
                explanation = "KMP alone typically keeps UI native per platform; Compose Multiplatform additionally shares the UI code itself.",
            ),
            QuizQuestion(
                id = "compose-multiplatform-q3",
                topicId = "compose-multiplatform",
                question = "Which platforms can Compose Multiplatform target?",
                options = listOf(
                    "Android only",
                    "Android and iOS only",
                    "Android, iOS, desktop (JVM), and web (via Kotlin/Wasm)",
                    "Only web browsers",
                ),
                correctIndex = 2,
                explanation = "Compose Multiplatform can render the same @Composable UI on Android, iOS, desktop, and web via Kotlin/Wasm.",
            ),
        ),
        tutorFocus = "This is the payoff topic for the whole category — the learner already knows @Composable functions from this app's own UI, so make that connection explicit. Contrast clearly with the earlier Kotlin Multiplatform topic: 'shares logic' vs 'shares logic and UI.'",
    ),
)
